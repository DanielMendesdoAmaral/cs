using System;

namespace Aula_17___Arrays
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] inteiro = new int [8]; //Essa variável pode guardar 9 valores (0-8) inteiros, mas ainda não foi inicializada ou atribuída.
            string[] caracteres = new string [4] {"Lauro", "Maria", "Juninho", "Daniel"}; //Essa variável guarda 4 valores do tipo string, já tendo sido inicializada.
            bool[] valores = {true, false}; //Essa variável guarda 2 valores do tipo bool, já tendo sido inicializada. A única diferença do exemplo anterior é que aqui não usamos new.     
        }
    }
}
